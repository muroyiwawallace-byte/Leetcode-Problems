int maxProfit(int* prices, int pricesSize) {

    int i,j,l;
    int s=prices[0];
    int m=0;
    for(i=1;i<pricesSize;i++){
        if(prices[i]<s){
            s=prices[i];
        }
        else if(prices[i]-s>m){
            m=prices[i]-s;
        }
    }
    return m;
}
    
